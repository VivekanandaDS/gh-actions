Name : GithUb Actions Workflow
on: workflow_dispatch
branches:
  - "*"
jobs:
    runs-on: ubuntu
    steps:
      - name: Install Python
        run: |
          sudo pip install python
          sudo pip install pandas
